ALTER TABLE accounts DROP CONSTRAINT IF EXISTS accounts_role_check;
ALTER TABLE accounts ADD CONSTRAINT accounts_role_check CHECK(role IN ('USUARIO','ADMINISTRADOR','DIRECTIVO'));
ALTER TABLE accounts ALTER COLUMN name TYPE TEXT;
ALTER TABLE accounts ALTER COLUMN email TYPE TEXT;
ALTER TABLE accounts ADD COLUMN email_lookup VARCHAR(64);
CREATE UNIQUE INDEX idx_accounts_email_lookup ON accounts(email_lookup);
ALTER TABLE accounts ADD COLUMN created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE accounts ADD COLUMN updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP;
ALTER TABLE students ALTER COLUMN full_name TYPE TEXT;
ALTER TABLE school_students ALTER COLUMN document_number TYPE VARCHAR(64);
ALTER TABLE school_guardians ALTER COLUMN document_number TYPE VARCHAR(64);
CREATE TABLE audit_events (
 id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
 actor VARCHAR(80) NOT NULL,
 operation VARCHAR(20) NOT NULL,
 resource VARCHAR(60) NOT NULL,
 record_id VARCHAR(80) NOT NULL,
 changed_fields TEXT NOT NULL,
 created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);
