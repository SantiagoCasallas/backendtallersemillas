package com.tallersemillas.backend.school.domain;

/** Aggregate kinds; persistence table names remain inside the adapter. */
public enum RegistryKind { STUDENT, GUARDIAN, HISTORY, MEDICAL_ENTRY, SCHEMA, SLOT, INTERVIEW, INSCRIPTION, INVITATION, OBSERVATION, YEAR_ENROLLMENT, SCHEDULE, PHOTO }
